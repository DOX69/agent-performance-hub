name: Agent Skills Testing

on:
  push:
    paths:
      - '.agent/skills/**'
      - '.agent/methodology/**'
  pull_request:
  schedule:
    - cron: '0 8 * * 3'  # Mercredi 8h UTC

jobs:
  test-gemini-3:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install google-generativeai pytest

      - name: Run Gemini 3 tests
        env:
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
        run: |
          echo "Running Gemini 3 tests (placeholder)..." 
          # pytest tests/test_gemini_skills.py -v

  test-claude:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install anthropic pytest

      - name: Run Claude Opus/Sonnet tests
        env:
          # ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          echo "Running Claude tests (placeholder)..."
          # pytest tests/test_claude_skills.py -v
